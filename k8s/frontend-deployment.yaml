apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: series-tracker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: sara583/series-tracker-frontend:latest
          ports:
            - containerPort: 5173